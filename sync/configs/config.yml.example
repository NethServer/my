metadata:
  name: "my-rbac-config"
  version: "1.0.0"
  description: "RBAC configuration for my application"

hierarchy:
  organization_roles:
    - id: admin
      name: "Administrator"
      type: user
      priority: 0
      permissions:
        - id: "admin:systems"
          name: "Administer systems"

  user_roles:
    - id: support
      name: "Support"
      type: user
      priority: 1
      permissions:
        - id: "read:systems"
          name: "Read systems"

  resources:
    - name: "systems"
      actions: ["create", "read", "update", "delete", "admin"]

  # Third-party applications (optional)
  third_party_apps:
    - name: "myapp.example.com"
      description: "My third-party application"
      display_name: "My App"
      redirect_uris:
        - "https://myapp.example.com/callback"
        - "https://myapp.example.com/auth/logto/callback"
      post_logout_redirect_uris:
        - "https://myapp.example.com"
        - "https://myapp.example.com/logout"
      # Custom scopes (optional - defaults to: profile, email, roles, urn:logto:scope:organizations, urn:logto:scope:organization_roles)
      # scopes:
      #   - "profile"
      #   - "email"
      #   - "roles"
      #   - "urn:logto:scope:organizations"
      #   - "urn:logto:scope:organization_roles"
      #   - "custom:read"
    
    # Example with custom scopes
    # - name: "api.example.com"
    #   description: "API service application"
    #   display_name: "API Service"
    #   redirect_uris:
    #     - "https://api.example.com/auth/callback"
    #   scopes:
    #     - "profile"
    #     - "email"
    #     - "api:read"
    #     - "api:write"

